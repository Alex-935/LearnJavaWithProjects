public class __10_interfacesAndAbstractClasses {

    public static void main(String[] args) {

        /*
        Interfaces
            - multiple interfaces can be implemented by another class.
            - names often end in "able"
            - are abstract and cannot be used with new
            - by default, interfaces and methods are public and abstract
            - methods are not abstract is denoted private, default or static.

            -variables are public, static and final by default
            - default and static methods need code-blocks
            - static methods aren't static by default

            tagging interface - has no methods, used for type information using instanceof

            - default allows methods to have an implementation that gets inherited by classes. I can overriden or left as its default.
            - classes do not inherit classes modified as static. To access a static method, you need to use InterfaceName.staticMethodName();
         */
    }
}
